{
  "permissions": {
    "allow": [
      "Bash(node -e \"const p = require\\('./package.json'\\); const deps = {...p.dependencies, ...p.devDependencies}; ['expo-camera','expo-av','expo-image-picker','expo-notifications','@expo/vector-icons'].forEach\\(k => console.log\\(k, deps[k] || 'NOT INSTALLED'\\)\\)\")",
      "Bash(node -e \"const p = require\\('./package.json'\\); console.log\\('expo-notifications:', p.dependencies['expo-notifications']\\)\")"
    ]
  }
}
